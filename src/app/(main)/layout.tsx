import React, { ReactNode } from "react";

const MainLayout = ({ children }: { children: ReactNode }) => {
      // manxe onboarded xainw vane teslai onboarding page ma pathaune
  return <div>{children}</div>;
};

export default MainLayout;
